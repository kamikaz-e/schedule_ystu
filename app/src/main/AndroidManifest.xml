<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.misha.myapplication">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".ScheduleApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning"
        tools:replace="android:appComponentFactory"
        android:appComponentFactory="whateverString"
       >
        <activity
            android:name=".activity.ActivityStartSettings"
            android:label="@string/title_activity_start_settings" />
        <activity
            android:name=".activity.ActivityStart"
            android:label="@string/title_activity_welcome_activity" />
        <activity
            android:name=".activity.ActivitySettings"
            android:label="@string/title_activity_settings" />
        <activity
            android:name=".activity.ActivityCallsSchedule"
            android:label="@string/title_activity_call_schedule" />
        <activity
            android:name=".activity.ActivityEditData"
            android:label="Редактор данных" />
        <activity android:name=".activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <provider
            android:authorities="com.example.misha.myapplication.provider"
            android:name=".database.AppContentProvider"
            android:exported="false"
            android:multiprocess="true"/>

    </application>

</manifest>